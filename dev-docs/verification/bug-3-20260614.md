---
kind: bug
id: 3
status_target: FIXED
commit_sha: fd3508f8496f4ee43880684b7a02055a841dd5ca
app_version: 0.2.2 (build 4)
date: 2026-06-14
verifier: claude
device_or_simulator: "iPhone 17 Pro Simulator"
os_version: "iOS 26.x"
build_configuration: Debug
backend: "n/a (audio-session config + route mapping)"
result: pass
---

# Bug #3 (GH #5) — Bluetooth mic + route-add — verification

## Acceptance criteria
| # | Criterion | Observed | Pass/Fail |
|---|-----------|----------|-----------|
| 1 | AirPods/Bluetooth mic available for capture | `.playAndRecord` now includes `.allowBluetoothHFP` (+ `.allowBluetoothA2DP`). Builds against iOS 26. | pass (build); device confirm deferred |
| 2 | Route additions are handled, not ignored | `.newDeviceAvailable`/`.override`/`.categoryChange` → `.routeChanged` → session stops (re-tap resumes), consistent with the interruption policy. `AudioSessionControllerTests` cover the mapping. | pass |
| 3 | Frequent minor route events don't spuriously stop | `.routeConfigurationChange` maps to nil (ignored) — covered by test. | pass |

## Commands run
```
xcodebuild build ...  # SUCCEEDED
scripts/run-tests.sh vrecorderTests  # 41 tests / 8 suites SUCCEEDED
```

## Observations
The route→event mapping is pure and unit-tested. Live AirPods connect/disconnect
behavior needs a physical device (simulator has no Bluetooth audio); GH #5 stays
open with `awaiting-device-verification` for that confirmation. Auto-reconfigure-
and-continue on the new device (vs. stop+re-tap) is a UX enhancement adjacent to
feature #3, not part of this fix.

## Artifacts
- `AudioSessionControllerTests` (3 tests), 41-test green suite.
