---
kind: bug
id: 2
status_target: FIXED
commit_sha: pending
app_version: 0.2.3 (build 5)
date: 2026-06-15
verifier: claude
device_or_simulator: "iPhone 17 Pro Simulator"
os_version: "iOS 26.x"
build_configuration: Debug
backend: "n/a (recognizer streaming — code review + regression)"
result: pass
---

# Bug #2 (GH #4) — unbounded partial stream + Task accumulation — verification

## Acceptance criteria
| # | Criterion | Observed | Pass/Fail |
|---|-----------|----------|-----------|
| 1 | Partial stream is bounded | `start()` uses `AsyncThrowingStream(bufferingPolicy: .bufferingNewest(64))` — a stalled consumer drops oldest (replaceable) partials, not grows. | pass (review) |
| 2 | No per-callback main-actor Task pile-up | Recognition callback yields DIRECTLY to the Sendable continuation; only rare final-rotate / error paths hop to the main actor via Task. | pass (review) |
| 3 | Finals preserved | Finals are rare vs the 64 buffer; post-finish yields are safe no-ops. Guaranteed-final coalescing is feature #4's pump. | pass (in practice) |
| 4 | No regression | 41 tests green; build clean. | pass |

## Commands run
```
xcodebuild build ...  # SUCCEEDED
scripts/run-tests.sh vrecorderTests  # 41 tests SUCCEEDED
```

## Observations
The recognizer's live streaming isn't unit-testable without a real
SFSpeechRecognizer + audio harness; fix is verified by code review + regression.
A guaranteed-final coalescing pump belongs to the feature-#4 VAD redesign.
GH #4 stays open `awaiting-device-verification` for a long-session memory check.

## Artifacts
- 41-test green suite; AppleSpeechRecognizer.swift diff.
