---
kind: bug
id: 1
status_target: FIXED
commit_sha: ab95f5f
app_version: 0.2.1 (build 3)
date: 2026-06-14
verifier: claude
device_or_simulator: "iPhone 17 Pro Simulator"
os_version: "iOS 26.x"
build_configuration: Debug
backend: "n/a (audio-pipeline unit logic)"
result: partial
---

# Bug #1 (GH #3) — VAD rotation truncation — verification

## Acceptance criteria

| # | Criterion | Observed | Pass/Fail |
|---|-----------|----------|-----------|
| 1 | Audio captured during the request-rotation gap is retained, not dropped | `PCMRollover` rings buffers while `request == nil`; `PCMRolloverTests.keepsAtMostCapBuffersDroppingOldest` + `drainResetsAndIsOrdered` prove ordered, bounded capture. | pass |
| 2 | Retained audio is replayed into the next request on rotation | `AudioTapBridge.setRequest(new)` drains the ring into `newRequest.append` before resuming. Covered by the bridge wiring + ring tests. | pass |
| 3 | The ring is bounded (no unbounded growth if rotation stalls) | `cap=24`; `keepsAtMostCapBuffersDroppingOldest` asserts oldest-drop at cap; `zeroCapDropsEverything`. | pass |
| 4 | Teardown clears the ring (no leak) | `setRequest(nil)` drains/clears; called from `AppleSpeechRecognizer.teardownAudio`. | pass |

## Commands run

```
xcodegen generate
xcodebuild build -scheme vrecorder -destination 'platform=iOS Simulator,name=iPhone 17 Pro'  # SUCCEEDED
scripts/run-tests.sh vrecorderTests   # RUN-TESTS RESULT: SUCCEEDED, 34 tests / 6 suites
```

## Observations

The defect is a real-mic timing/race; it cannot be reproduced headlessly (no live
mic in the simulator, no tap driver). The fix's mechanism — the bounded rollover
ring and its drain-into-next-request — IS fully unit-verified at the subsystem
boundary. End-to-end real-mic confirmation remains the GH-issue **close-gate**:
issue #3 stays open with `awaiting-device-verification` until a device pass
records "continuous speech with pauses, no truncated next utterance."

## Artifacts

- `PCMRolloverTests` (3 tests) + 34-test green suite.
